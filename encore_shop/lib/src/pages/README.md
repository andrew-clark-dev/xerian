# Entity pages

Contains all the pages to support the business entities.

## Getting Started

Use https://pub.dev/packages/json_annotation to create the business entities.

Genrate the entities from the annotations as described on https://github.com/google/json_serializable.dart/tree/master/example

i.e. run "dart run build_runner build" in the project home directory
